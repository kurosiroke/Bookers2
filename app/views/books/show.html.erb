
<% if @book.errors.any? %>
  <%= @book.errors.count %>件のエラーが発生しました
  <ul>
    <% @book.erroes.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<% @books.each do |book| %>
  <div>
    <%= link_to book_path(book.id) do %>
      <%= image_tag book.get_image %>
    <% end %>

    <p>投稿ユーザー画像：<%= image_tag book.user.get_profile_image(100,100) %></p>
    <p>ショップ名：<%= book.shop_name %></p>
    <p>説明：<%= book.caption %></p>
    <p>ユーザーネーム：<%= book.user.name %></p>
    <% if @book.user == current_user %>
      <%= link_to　"削除". boook_path(@book), method: :delete %>
    <% end %>
    <%= link_to "編集", edit_book_path(@book.id) %>
    <%= link_to "削除", book_path(@book.id) method: :delete, "deta-confirm" -> "本当に削除しますか" ？%>
  </div>
<% end %>